# filesystems salt formula

This formula prepares (formats) and mounts Linux filesystems.

See `pillar.example` for more information on howto declare devices, mountpoints and options using the salt-pillar.
